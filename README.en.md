> 🇯🇵 [日本語](./README.md)

# @hayashiii/pdfmint

[![npm version](https://img.shields.io/npm/v/@hayashiii/pdfmint.svg)](https://www.npmjs.com/package/@hayashiii/pdfmint)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

An agent-friendly CLI that turns HTML or Markdown drafts into polished PDF deliverables with dependable Japanese typography.

```text
HTML / Markdown
       ↓
    pdfmint
       ↓
PDF + preview PNG + JSON result
```

## Quick start

```bash
npm install -g @hayashiii/pdfmint
pdfmint report.md report.pdf --font shippori --json
```

Requires Node.js 22+.

## Tool family

| Task | Tool |
|---|---|
| Collect public websites into screenshot archives | [`sitesnap`](https://github.com/hayashiii-ghub/sitesnap) |
| Verify UI states, responsive layouts, and implementation quality | [`shimon`](https://github.com/hayashiii-ghub/shimon) |
| Turn an HTML or Markdown draft into a PDF deliverable | `pdfmint` |

pdfmint compiles one finished draft. It is not a document authoring agent, batch runner, or template manager.

## Example output

The repository includes a fictional [release decision brief with its Markdown source, PDF, and preview PNG](https://github.com/hayashiii-ghub/pdfmint/tree/main/demo). It demonstrates Shippori Mincho, tables, a callout, a footnote, and a one-page assertion.

![Release decision brief generated by pdfmint](https://raw.githubusercontent.com/hayashiii-ghub/pdfmint/main/demo/release-brief.png)

## Fonts

Four Japanese families are bundled locally and never fetched at runtime:

| ID | Family | Best for |
|---|---|---|
| `zen` | Zen Kaku Gothic New | Practical documents and tables (default) |
| `shippori` | Shippori Mincho | Formal reports and proposals |
| `kiwi` | Kiwi Maru | Friendly notices |
| `klee` | Klee One | Letters and soft one-page documents |

Font selection applies to Markdown. HTML keeps its own CSS.

## Usage

```bash
pdfmint <input.html|input.md> <output.pdf> [options]
```

Important options:

- `--font <zen|shippori|kiwi|klee>`
- `--css <file.css>` for Markdown overrides
- `--format <A4|A3|Letter|Legal>`
- `--margin <value>` and `--landscape`
- `--png <output.png>` with `--viewport` and `--scale`
- `--expect-pages <number>`
- `--json` for structured agent output

Markdown supports GFM, frontmatter titles, footnotes, GitHub callouts, syntax highlighting, and aligned tables.

MIT licensed. Bundled fonts retain their SIL Open Font License 1.1 notices under `dist/fonts/`.
